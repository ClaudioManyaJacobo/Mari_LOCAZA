<%= form_with(model: facultad) do |form| %>
  <% if facultad.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(facultad.errors.count, "error") %> prohibited this facultad from being saved:</h2>

      <ul>
        <% facultad.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :id_facultad, style: "display: block" %>
    <%= form.text_field :id_facultad %>
  </div>

  <div>
    <%= form.label :nombre, style: "display: block" %>
    <%= form.text_field :nombre %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
