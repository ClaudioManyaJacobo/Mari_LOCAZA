<%= form_with(model: alumno) do |form| %>
  <% if alumno.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(alumno.errors.count, "error") %> prohibited this alumno from being saved:</h2>

      <ul>
        <% alumno.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :codigo, style: "display: block" %>
    <%= form.text_field :codigo %>
  </div>

  <div>
    <%= form.label :nombre, style: "display: block" %>
    <%= form.text_field :nombre %>
  </div>

  <div>
    <%= form.label :edad, style: "display: block" %>
    <%= form.number_field :edad %>
  </div>

  <div>
    <%= form.label :facultad_id, "Facultad" %>
    <%= form.collection_select :facultad_id, @facultades, :id, :nombre, prompt: "Selecciona una facultad" %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
